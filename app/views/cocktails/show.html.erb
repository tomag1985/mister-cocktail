	<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= cl_image_path @cocktail.photo.key, crop: :fill %>');"> 
		<div class="container">
				<h1><%= @cocktail.name %> </h1>
		</div>
	</div>
		


<div class="container mt-3">
	<div class="row">
		<div class="col-7 mr-3 p-3">
			<h2>Descripción</h2>
			<ul style="list-style-type:none">
				<% @cocktail.doses.each do |dose| %>
					<li><%= dose.description %> </li>
				<% end %>
			</ul>
		</div>

		<div class="col-4 p-3">
			<h2>Ingredientes</h2>
			<ul style="list-style-type:none">
				<% @cocktail.doses.each do |dose| %>
					<li><strong><%= dose.ingredient.name %></strong> <%= link_to "delete", dose_path(dose), method: :delete%> </li>
			<% end %>
			</ul>
			
			<%= link_to "Agregar ingrediente", new_cocktail_dose_path(@cocktail), class:"btn btn-dark" %>
			
		</div>


	</div>
	
	<ul>




</div>