<div class="container w-50 mt-3">
	<h1>Cree aquí sus cantidades</h1>

	<%= simple_form_for [@cocktail, @dose] do |c| %>
	<%= c.input :description %>
	<%= c.association :ingredient %>
	<%= c.submit "Add cocktail", class:"btn btn-primary"%>
	<% end %>
</div>
